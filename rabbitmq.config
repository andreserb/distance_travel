%% rabbitmq.config

[
  {rabbit, [
    {loopback_users, []}  %% Allows remote access for default users like "guest"
  ]},
  {rabbitmq_auth_backend_internal, [
    {allow_remote_login, true}  %% Allow remote login
  ]}
].
